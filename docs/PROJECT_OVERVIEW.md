# Project Overview

Pipeline to process 500+ GB of media with strict deduplication, 10–15 GB batch uploads to a Syncthing folder, and post-sync EXIF sorting into `sorted/YYYY/MM/DD`. Entire flow is API-driven, modular, resumable, observable, and configurable.
